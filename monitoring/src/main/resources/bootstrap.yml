spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config:8888
      fail-fast: false
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
server:
  port: 8989